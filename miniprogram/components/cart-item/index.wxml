<view class="cart-item">
  <view class="check" bindtap="onToggle">
    <image src="{{checked ? '/assets/checked.png' : '/assets/unchecked.png'}}" class="check-icon" />
  </view>
  <image class="img" src="{{item.image || '/assets/placeholder.png'}}" mode="aspectFill" />
  <view class="info">
    <text class="name">{{item.product_name}}</text>
    <view class="price-row">
      <text class="price">{{item.price}}</text>
      <view class="qty">
        <view class="btn" bindtap="onMinus">-</view>
        <text class="num">{{item.quantity}}</text>
        <view class="btn" bindtap="onPlus">+</view>
      </view>
    </view>
  </view>
</view>
